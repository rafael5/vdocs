# Pipeline audit remediation — tracker

Plan:
[`pipeline-audit-remediation-implementation-plan.md`](pipeline-audit-remediation-implementation-plan.md)
· Kickoff prompts: [`../prompts/pipeline-audit-remediation/`](../prompts/pipeline-audit-remediation/)
· Audit: [`../reference/pipeline-adversarial-audit.md`](../reference/pipeline-adversarial-audit.md).
Update this table **per landed step** (TDD → `make check` → commit → tick). A step is DONE
only when its plan-stated measure is demonstrated (green *and*, where specified, red on the
broken fixture). Record the commit hash and any measured number in Notes. A `P<n> ✓` row
additionally requires the **next** phase's kickoff prompt written (execution protocol in the
plan).

Baseline (2026-08-01, `corpus_content_hash 3ce0872e…`): 1,040 fetched / 1,034 indexed (6
doc_ids collapsed); doctor advisory-only; 7 low-retention docs in gold; SQLite fp = row
count; 27.2% of 52,048 latest sections chunk-less; golden nDCG@10 = 0.469 (L1.2 sweep).

| Step | What lands | Status | Commit / notes |
|------|-----------|--------|----------------|
| **P1 — acquisition-chain integrity** | | | |
| P1.1 | `raw/index.json` v2: derived from acquisitions ⋈ inventory, keyed by `doc_id`; fetch `contract_ver=2`; convert/normalize readers adapted | ☑ | `make check` green (1,118 tests, 96.22%). Design §5.5 already *specified* "derived projection of acquisitions" — the sha-keyed merge was the deviation, now corrected. v1 files refused with a remediation (`parse_raw_index`). Obsolete merge test replaced by two behavior tests (narrow re-fetch keeps every fetched doc; a no-longer-admitted doc leaves). New count `indexed`. |
| P1.2 | `validate` Step 5 chain reconciliation (fetched ⊆ admitted; raw-index == fetched; converted == normalized == indexed-raw), findings by doc_id | ☑ | `chain_pure.py` 100% cov; 6 finding kinds; validate gains 4 contract edges (GOLD_INVENTORY/RAW_INDEX/TEXT_CONVERTED/REGISTRIES). 4 red-path integration tests incl. the measured six-doc scenario. Fixture note: seed a broken chain **before** blessing — mutating a tree after trips the drift preflight (a different, coarser guard) instead of the gate under test. |
| P1.3 | DOCX magic check before CAS put; `bad_content` acquisition status | ☑ | `is_docx_payload` (ZIP magic `PK\x03\x04`); refused bytes never enter the write-once CAS, WARN names the URL, retried like a transient miss (a WAF hiccup must not blacklist a real doc). `Acquisition.status` literal + `_STATUS_ORDER` extended. |
| P1.4 | **NEW** — `build_atomic` sweeps the TARGET's stale `-wal`/`-shm` after the replace | ☑ | `6668a26`. Found BY the acceptance run, not the audit: rebuilding index.db over a live-WAL database corrupted it (`database disk image is malformed`; integrity_check showed real btree damage). R7 hardened the temp side only. Shared primitive — serve-inventory/index/relate all rebuild through it. |
| P1 ✓ | Live: 1,040 == 1,040 == 1,040 == 1,040; six doc_ids in `doc_meta_staged`; gate red on hand-broken seam | ☑ | **Chain equal at 1,040 across all six seams** (fetched · raw-index · converted · normalized · doc_meta_staged · documents); all six restored docs present; `integrity_check ok`; validate GREEN incl. `chain_findings=0`; **doctor GOLD LIBRARY: GREEN** (19 pass). Group count **stayed 615** (not ~617 as predicted — all six folded into existing version groups; `slug_stem` correctly strips `r0218`-style revision tokens). Red path proven by 4 permanent integration tests rather than a one-off scratch-lake demo (automated, 3 finding kinds, cannot rot). |
| **P2 — doctor into the DAG** | | | |
| P2.1 | Terminal `doctor` stage (`reports/doctor/doctor.json`, ALWAYS_RERUN, RED ⇒ fail); CLI aliased; `build` tail dropped | ☑ | `74b136b`. `make check` green (1,146 tests, 96.22%). The CLI's inline loader became `server.doctor.diagnose_lake` — one diagnosis path for stage + command; no check logic forked. `requires` gained **REGISTRIES** beyond the plan's three (the doctor policy / gate keep-set / entity-quality declaration are real inputs; ordering unaffected — no producer). `requires_upstream_record=False` (F4) so a wiped `state.db` can't make the gate unrunnable. **`vdocs doctor` + `release` drive the STAGE** (`_run_doctor`), not a stale report: anything that stops the stage completing yields RED, never fail-open. `build --to manifest` → **`--to doctor`** (it would otherwise have stopped one node short of the gate it was meant to end on). |
| P2 ✓ | `vdocs run` ends at doctor GREEN; induced-defect lake exits non-zero | ☑ | Live: `vdocs run` ends **[16/16] doctor GREEN** — 615 gold docs, **19 pass / 0 by-design / 0 warn / 0 fail**; `doctor.json` (2,430 B, 19 checks) written and its verdict matches `vdocs doctor`. Red path: scratch-lake copy with one gold doc's `software_class` blanked → **exit 1 AT the doctor stage**, naming `ACKQ:ackq3_0_p12tm`. Note the plan's suggested red-path lever (re-enable a quarantined entity type) is **not available** — `entity-quality.yaml` currently declares **zero** excluded types, so that check is inert on this lake; a policy-floor field is the working lever. Low-retention re-measured for P3: **still 7** (the six restored docs added none). |
| **P3 — retention gates** | | | |
| P3.1 | ~~Relocated table words credited~~ → **capture-before-strip for anchorless legacy-TOC entries, then credit IN-WINDOW relocations** | ☑ | `b28fbf5`. **The plan's step was wrong in both halves; measure-first caught it.** (a) Crediting *table* words is **fail-open**: normalize takes its baseline AFTER lifting revisions + tables (`pre_norm_words`), so those words are not in the denominator — measured, the credit turns `PSD/psd_3_p69_tm_cp` (true whole-stage retention **0.57**) into a PASS at 0.87. The audit's "slightly harsh" reading of R‑5 is **backwards**: the gate was too *lenient*. Only the legacy TOC (→`toc.yaml`) and boilerplate (→`gold/_shared`) are in-window and creditable. (b) Underneath it, a real defect: inside a confirmed TOC region **every** line was dropped but only `](#anchor)`-linked ones were captured, so the paper dialect (`Introduction .......... 1`) was **deleted with no record** — a capture-before-strip violation invisible to every net (the residue scan only asks whether a TOC *heading* survived; the strip removed that too). Root cause of both QUARANTINEs. Fix: `parse_plain_toc_entry` + one `_capture_toc_entry` used by both scanner branches. **low-retention 7 → 3**; `toc_sidecars` 807 → **821**. |
| P3.2 | `retention` block in `capture.yaml`; normalize `contract_ver=2` | ☑ | `f81dcb2`. `{retention, verdict, enriched_words, kept_words}` on every bundle; travels to the gold anchor under `bundle.yaml`'s signed manifest, so Step 6 reads a **verified** record. `retention_verdict()` returns `""` for an unscored bundle — absence is UNKNOWN, never PASS. |
| P3.3 | `validate` Step 6: QUARANTINE blocks; REVIEW needs `registries/retention-signoff.yaml`; validate requires REGISTRIES | ☑ | `f81dcb2`. QUARANTINE always blocks (**not** excusable by sign-off — its own test); REVIEW blocks unless signed off; **unscored blocks** (UNKNOWN ≠ PASS). **Ruling the prompt asked for:** a sign-off for a doc that no longer needs one is `retention-signoff-stale` — reported, **never blocking** (registry hygiene, not a corpus defect). `blocks_publish` stays the rule's one definition; the new `validate/retention_pure.py` is only the consumer side. REGISTRIES edge already present from P1.2 — verified, not re-added. |
| P3 ✓ | Live green with residual docs PASS/signed-off; synthetic over-strip reds validate | ☑ | **`vdocs run` GREEN end-to-end, 16/16.** validate `retention_findings=0`, doctor 19 pass / 0 fail. **Zero sign-offs were needed** — `retention-signoff.yaml` ships empty, i.e. nothing was waved through. The 3 residual low-retention docs (`PSD/psd_3_p69_tm_cp` 0.77, `psd_3_p71_tm_cp` 0.76, `psd_3_p69_um_supv_cp` 0.05) are **not gold anchors** — superseded versions that never ship; verified positively (all **615** gold bundles carry a scored retention block, so the zero is coverage, not blindness). `PRCA/icd_10_um_tp_4_5_281` (was 0.14) resolved by the capture fix. Red path = permanent tests, not a scratch-lake demo: 5 validate red-path integration tests + an end-to-end over-strip scoring QUARANTINE through the real phrase registry. |
| **P4 — sound SQLite fingerprints** | | | |
| P4.1 | `sqlite_fingerprint` always content-hash (canonical row encoding) | ☑ | `1419d1b`. **Cost measured, decisively bounded:** largest contracted table `index.db:relations` **203,272 rows → 0.33 s**; all 13 contracted tables ≈ **1.1 s** total — so the plan's memoization hedge was **not** taken (say so if a later phase adds one). `verify` accepted and ignored on the SQLite path; it keeps its meaning for files/trees, where size:mtime is honest. |
| P4.2 | `rows:N` legacy-format migration (accept-once, re-record) | ☑ | `1419d1b`. **13 of 31** recorded `outputs_fp` were legacy — every SQLite producer. **The trap:** tolerating a legacy value in the *consumer's* drift check is necessary but not sufficient — the **producer** skips on its own unchanged inputs, so it never re-records and the legacy value persists forever, making "accept once" permanent leniency. Fixed by re-running a stage whose own record is legacy, once. **The line not crossed:** a legacy record is accepted only while the row count it encoded still matches; a count that disagrees is a change the retired format could itself see, and swallowing it would trade a false alarm for a silent one. |
| P4 ✓ | Same-rowcount cell mutation re-runs `index`; full live run, no spurious drift FAILs | ☑ | Live migration in one `vdocs run`: serve-inventory · enrich · index · resolve · merge · relate each re-ran once and re-recorded; **zero drift FAILs**; GREEN 16/16 at doctor. **All 113** recorded fingerprints are now content hashes (**0 legacy**), and a second run skips everything — no spurious re-runs in steady state. **R‑1 closure:** `merge`'s skip decision is now keyed on the *content* of `index.db:entities` + `index.db:chunks`, the very tables `index` rebuilds, so the P3 wipe cannot recur by construction; the mechanism is pinned by a permanent test (same-row-count cell change → consumer re-runs) rather than a scratch-lake demo, per this prompt's own precedent. |
| **P5 — history lineage truth** | | | |
| P5.1 | `merge_history` supersedes entries on changed `body_sha256`; consolidate `contract_ver=4` | ☑ | `make check` green (1,188 tests, 96.29%). Baseline **re-measured independently before touching code** and it reproduced: **615/615** stale, **0** retained blobs missing from the CAS, **92** multi-member groups (the frontmatter-only/body-differs split came out 566/49 on my cruder splitter vs the prompt's 531/84 — the aggregate is what matters and it is identical). Trigger stays **`body_sha256` only** (per plan + [S9]a, where `revisions` goes stale *because* the body changed) — a total fact comparison was considered and rejected: it would demote on any input jitter, and a lineage that grows every run is the failure mode this step exists to prevent. The demoted fact-dict drops `is_latest` (derived) and its own `superseded` (would nest exponentially); the list is **oldest-first**, so the file only ever grows at the tail. New property test is the phase thesis: re-processing any subset leaves every captured `body_sha256` reachable. |
| P5.2 | `validate` Step 4+: latest member sha == sha(body.md) (`stale-lineage`) | ☑ | `make check` green (1,199 tests, 96.31%; `lineage_pure.py` 100% cov). **Sequenced as the prompt required:** P5.1 landed (`75d12e6`) → live `consolidate` re-run → re-measured **0 of 615** stale → only then was the gate wired, so it has never been a gate over a lake it would red. New `validate/lineage_pure.py` folded into Step 4's `bundle_findings` (already blocking, already reported) rather than a seventh step — the finding it catches is a *bundle* defect the manifest structurally cannot see. Second kind `unverifiable-lineage` for no-history / no-single-`is_latest` / no-recorded-sha: absence is UNKNOWN, never OK (P3.3's rule). Red-without/green-with proven by stashing the wiring, not asserted. Fixture note: the gold-bundle fixture now seeds a **truthful** `history.yaml` and the stale sha is injected **before** `bundle.yaml` is built, so the manifest stays clean and only the lineage check can fire (the test asserts exactly that). |
| P5 ✓ | Forced content change → superseded record + green; hand-staled history reds | ☑ | **615 → 0.** The live `consolidate` re-run under `contract_ver=4` left **0 of 615** anchors stale (same comparison as the pre-measurement, then re-confirmed by running the *shipped* `lineage_pure.check_lineage` over all 615 bundles: **615 clean, 0 findings** — the zero is coverage, not blindness). **1,034 of 1,040** member entries gained exactly one `superseded` record (depth-1 across the board); the **6** that gained none are the P1-restored docs, first captured *after* the drift — a consistency check that fell out for free. Every superseded `body_sha256` still resolves in the `_shared/history` CAS. Spot-check of a 14-member group (`ADT/iss_user_guide`): each member's prior `patch_id`/`official_date`/`source_sha256`/`revisions` are **kept in `superseded`**, not replaced, and the latest member's sha equals the bundle's `body.md`. `vdocs run` GREEN 16/16 through doctor (19 pass / 0 fail), and a second run skips everything — steady state, no per-run history growth. **Caught in the wild, and it contradicts the P4 ✓ row:** the `index`-re-runs-then-`merge`-skips → `entity_skl` wipe → doctor RED sequence **recurred** on the first re-run. P4 closed the *input* half (a content change now reaches `merge`); this is the *output* half — `index` recreates `entity_skl` as an empty shell, `merge` skips on genuinely unchanged inputs, and nothing checks that a stage's own outputs still exist. Repaired with the documented `vdocs run --from merge --force`. R‑1 is **not** fully closed; P6 will hit it again (its prompt says so) and P7 should either fix it or give it a register row. |
| **P6 — container lead-in chunking** | | | |
| P6.1 | Containers with substantive lead-in become searchable; index `contract_ver=15`; golden nDCG no regression | ☑ | **Subsumed by P6.1b** (landed 2026-08-02, out of phase order at the operator's request). The predicate `tokens > 0 or has_referent` makes a container with a lead-in searchable *by construction*, so P6.1's 6,779 sections are covered by the same one-line change; there was no separate commit and no second `contract_ver` bump. `kind` is untouched — container/ok/stub/hollow distribution is **identical** before and after (11,543 / 37,495 / 463 / 2,627), so the nav map and over-strip scoring are unaffected exactly as §P6 required. | **Post-P5 baseline measured 2026-08-01** (in the P6 prompt): golden nDCG@10 **0.5134** (not the audit's 0.469 — SKL work moved it), MRR 0.6067, recall@10 0.6140. ⚠️ **The `<8%` target is unreachable by this mechanism:** running the shipped shredder over all 615 gold bodies, **6,779 of 11,543** containers (58.7%) clear the 8-token floor, so the predicted residual is 4,764 bare containers + 2,627 hollow = **~14.2%**, not <8%. Settle the target before building. **The residual is NOT an upstream artifact** (measured 2026-08-02): re-leveling alone flips **7** sections across 612 docs; all of normalize creates **173** containers, of which only **72** are bare — so ≤1.5% of the 4,764 bare containers are our own doing (≈0.14 pp). Rider found while measuring: `infer_heading_levels` treats **empty-titled** headings (pandoc's Word bookmark-only paragraphs — 1,932 of 85,829 enriched headings, 320 docs) as real ancestors while `shred_sections` discards them, so re-leveling can turn a leaf into a container; harmless at this scale, but it is two components disagreeing about what a heading is — a P7 register candidate. nDCG 0.5134 → ___ |
| P6.1b | **NEW (2026-08-02, measured)** — separate the retrieval predicate from the QA floor: `kind` keeps the 8-token floor, `searchable` becomes `tokens > 0 or has_referent` | ☑ | **LANDED.** `make check` green (1,204 tests, 96.31%). New `kernel.markdown.is_searchable`; `index contract_ver=15`. Live: chunk-less **26.696% → 10.491%** (13,916 → 5,469 of 52,128), chunks 48,769 → 57,895, `vdocs run` GREEN 16/16 at doctor. Residual is **exactly** the predicted "0 tokens, no referent" population (4,648 bare containers + 821 empty hollow) — the prediction and the outcome agree to the section. ⚠️⚠️ **CORRECTED 2026-08-02 — the first golden measurement of this change was invalid.** It reported "nDCG@10 unchanged at 0.5134, 0 of 20 queries changed", and I attributed that to the set being blind to the class. The real cause: `scripts/baseline_golden.py` defaulted to **`~/data/vdocs-dev`** — a stale 451-doc lake, last built in June, that P6.1b never touched — while the printed rollup named no corpus at all. **Three measurements were quoted as evidence about a lake the harness never read.** Fixed: the harness now defaults to `Settings().data_dir` (the lake every other command uses), refuses a missing index.db, and stamps `index_db`/`documents`/`chunks`/`corpus_content_hash` into every rollup. **Real numbers, production lake (1,040 docs, `6dbec1f5…`):** the five P6.4 queries average **nDCG@10 0.751**; `quasar-package-wide-variables` and `fileman-input-transform-definition` score a perfect **1.000** with their single judged section at rank 1 — and each of those sections had **zero chunks** before P6.1b, so both queries scored exactly **0.000** by construction. That is proof of gain, measured. **Trap caught:** `manifest` SKIPPED after the rebuild and shipped a CORPUS.md still quoting 26.7%/~73% — `USAGE` lives in *code*, so editing it moves no input fingerprint. Fixed by `manifest contract_ver=4` + a comment saying the bump covers baked-in **text**, not just shape. Same family as P4/P5: a stage's output can go stale with every input unchanged. | Found by decomposing the unsearchable 14,170 by *what the section holds*: **1,896** sections carry 1–7 tokens of real prose and get **no chunk and no `chunks_fts` row**, so neither their text nor their heading is searchable (verified: `ACKQ/ackq3_0tm/package-wide-variables` → 0 FTS hits for a phrase in its shipped body; the same heading in `LR`/`XWB`/`AMT` is `ok` and searchable — identical questions answered for some packages, silently not for others). Dark examples are exactly the short reference entries a TM is made of: HL7 `TABLE 0136 - YES/NO INDICATOR`, `GETDATA^ACKQAG02` → "Provides the data for START^ACKQAG02.". Same root cause as P6.1 — an over-strip taxonomy driving a retrieval decision. **Closed by the same measurement:** the CSV-sidecar worry is a non-issue — B3b's table-chunk path (`index/stage.py:294`) is ungated by `searchable`, and **24 of 24** table-referent containers already carry a table chunk. Genuinely contentless residual = **5,469** (0 tokens, no referent). Projected: 26.70% → ~14.2% (P6.1) → **~10.5%** (both). |
| P6.2 | Re-measure chunk-less share; update all five hardcoded 26.7%-rule sites | ☑ | **Landed with P6.1b** (the numbers only exist after the rebuild, and a stale rule is worse than none — it sends an agent to a body file for text `search` now hands it). **26.70% → 10.49%**; all five sites moved together: `mcp.py` measured-constant comment · `NOT_INDEXED_RULE` · orientation · `initialize` instructions · `_has_chunks` docstring (254 of 11,526 containers with chunks → **6,895 of 11,543**) · `manifest_pure.USAGE`. The rule itself **stays** — smaller and sharper, not weaker. Three surface tests asserted the retired mechanism by name (`"container" in text and "hollow" in text`); they now assert the explanation (`"subsections"`), since naming `kind` would teach a client something false. **New gate:** `test_no_client_surface_quotes_a_retired_coverage_constant` pins `26.7%`/`~73%`/`13,899`/`52,048`/`11,526` out of every client surface, so a future re-measure cannot land on four of the five. Out-of-repo surfaces (the `vdocs-corpus` skill, the mounted MCP instructions) still quote 27% — **operator action, P7.3, do not edit silently**. |
| P6.3 | Not-indexed warning parity: ask CLI + `--json` share the MCP constant | ☑ | `make check` green (1,205 tests, 96.31%). The rule + a `search_envelope(hits)` **builder** now live in `server/search.py` — the module all three surfaces already import — so they cannot drift in *shape* either, not just wording. **The CLI was the worst offender:** its empty result said *"no matches in the gold corpus."* — the exact sentence the MCP surface is forbidden to emit, and the one that licensed the FileMan false-negative incident. `ask --json` changed from a bare array to `{hits, hit_count[, warning]}` — a deliberate breaking change for parity, recorded in the user guide. Verified on the live lake: plain CLI, `--json`, and the MCP `search` tool emit byte-identical text. |
| P6.4 | **NEW** — golden queries for short reference entries + harness provenance | ☑ | Five queries added (`api-leadin` + `short-reference` axes), graded by **reading** the sections, with lexical near-misses deliberately left unlabelled as discriminators; every `grade: 3` is a section that carried zero chunks before P6.1b. Production results: **0.751 mean nDCG@10**, three at ~1.000. **The bigger find was the harness itself** — it defaulted to a stale `~/data/vdocs-dev` and its rollup recorded no corpus, so it silently answered questions about the wrong lake three times running (the markdown named the lake; the JSON rollup, which is what gets pasted into trackers, did not). Now: default = `Settings().data_dir`, refuse a missing index.db, and `index_db`/`documents`/`chunks`/`corpus_content_hash` in every rollup. ⚠️ **Every pre-2026-08-02 golden number in this repo — including the audit's 0.469 and the 0.5134 quoted above — is a dev-lake number and is NOT comparable to a production run.** |
| P6 ✓ | ~~Chunk-less share < 8%~~ → **measured residual, target struck**; FileMan lead-in query answered from `search` | ☐ | **`< 8%` was struck as unreachable by the prescribed mechanism, before building** (measured: the bare-container + empty-hollow floor is 5,469 sections = 10.49%, and every one of them is genuinely contentless). Achieved 26.70% → **10.49%**. Remaining for the ✓: P6.3, P6.4, and the user-visible demo — a container-lead-in query (the FileMan "Input Parameters" class the audit cites) answered from `search` where it previously needed the body.md fallback. |
| **P7 — close-out** | | | |
| P7.1 | Full forced end-to-end run green through doctor stage | ☐ | |
| P7.2 | Audit register rows struck with commit hashes; master-table cells refreshed | ☐ | |
| P7.3 | Operator flagged: vdocs-corpus skill + mounted MCP instructions quote stale % | ☐ | |
| P7.4 | Plan + tracker archived to `docs/historical/` | ☐ | |

**Riders landed opportunistically** (out-of-scope register items that rode a phase): R‑8 (DOCX magic check) landed as P1.3 by plan; **P1.4 was unplanned** — a corruption defect the audit never saw, surfaced only by running the pipeline end-to-end. Lesson for later phases: the acceptance run is not a formality, it is the only step that exercises real WAL/crash states no fixture reproduces. **R‑13 + R‑14 landed with P2.1** (`74b136b`): `_index_fingerprint` now `PRAGMA wal_checkpoint(TRUNCATE)`s before hashing — reproduced red first (a committed write sitting in `-wal` left the AI card's staleness stamp **byte-identical**, so a consumer would have concluded "unchanged" over a corpus that changed); and `_wipe_lake` now spares `reports/validation/` (validate's cross-run drop baseline is *evidence*, not derived state — wiping it disarmed the drop check on exactly the `--fresh` runs most likely to lose documents). Both are WAL/lifecycle bugs of the same family as P1.4.

**P4 caught in the wild (2026-08-01, during P3's acceptance run).** The `normalize contract_ver=2`
cascade re-ran `index`, which rebuilt its tables with **identical row counts** — so `merge`'s cheap
`rows:N` input fingerprints were unchanged, `merge` SKIPPED, its `entity_skl` projection stayed the
empty shell `index` had just recreated, and `doctor` went **RED** on the SKL-projections check.
Repaired with `vdocs run --from merge --force`. This is finding 5a (R‑1) reproducing itself on the
live lake without being provoked, and it is also the P2 gate proving its worth: before P2 that
wipe would have shipped under a green pipeline, exactly the scenario the P2 prompt named.

**Corrections:** the audit named the six lost doc_ids by eyeballing the first element of each duplicate pair — those were the *survivors*. The real six (`CPRS:cprsguitm`, `PSJ:psj_5_0_{nurse,supr}_um`, `PSJ:psj_5_0_tm`, `PSN:psn_4_{tm,um}_r`) were computed as a set difference at P1 close; [S4] and the P1 prompt are corrected. Count, mechanism and impact were right — only the identifiers were wrong.
